steps:
  - script: |
      python3 --version
      which python3
    displayName: "Verify Python on self-hosted agent"

  - script: |
      python3 -m pip install --upgrade pip
      pip install -r requirements.txt
    displayName: "Install Dependencies"